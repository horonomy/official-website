# HORO-40 — validation report

Local dev-server validation of the new `docs/marketing/ia-messaging-plan.md`
page ahead of merge. Captured with Playwright against `pnpm start --port 4040`.

## Screenshots

| File | What it shows |
|---|---|
| `01-homepage-unchanged.png` | Homepage (`/`) at 1200×900 — no regression. Hero, product tiles, philosophy, manifesto, footer, consent banner all intact. |
| `02-ia-messaging-plan-top.png` | New page (`/docs/marketing/ia-messaging-plan`) top-of-fold — frontmatter title applied, autogenerated sidebar picks up the marketing group, right-hand TOC renders every H2/H3. |
| `03-ia-messaging-plan-full.png` | Full-page render of the new page. |
| `04-docs-intro-regression.png` | Pre-existing `/docs/intro` — renders as before; auto-populates prev/next → new IA plan page (native Docusaurus behavior, no source change). |
| `05-mobile-375.png` | Mobile 375×812 — hamburger menu, breadcrumbs, collapsible TOC, no horizontal overflow. |

## Console

No console errors on either the new page or the regression check page.

## Observation for reviewer

The docs plugin is enabled on horonomy.dev, so the new file becomes a live public
route at `horonomy.dev/docs/marketing/ia-messaging-plan`. It is NOT linked from
the top navbar — the only way to reach it is direct URL or via the docs
sidebar once the visitor is on `/docs/intro`. Whether to keep it discoverable-
only-by-URL, hide it entirely, or promote it via navbar is a call worth
raising in review; the PR itself does not change navbar or footer.
